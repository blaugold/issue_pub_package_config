name: issue_pub_package_config
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.1
  local_package:
    path: packages/local_package

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.10.10

flutter:
  uses-material-design: true
  generate: true
